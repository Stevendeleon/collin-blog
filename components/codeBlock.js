export const pid_def = `  pid_code not found`;

//featured
export const pid_0 = `function helloWorld(){
    console.log('Hello World!');
};

helloWorld();`;

export const pid_1 = `#!/bin/bash
  
FILENAME="european-cities.txt"

LINES=$(cat $FILENAME)

for LINE in $LINES
do
    echo "$LINE"
done`;

export const pid_2 = `class Dog:
  def __init__(self, name, age):
     self.name = name
     self.age = age`;
